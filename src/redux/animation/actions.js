import { createAction } from '@reduxjs/toolkit'

export const reset = createAction(`ANIMATION/RESET`)
export const logoFaded = createAction(`ANIMATION/LOGOFADED`)
export const logoFading = createAction(`ANIMATION/LOGOFADING`)

